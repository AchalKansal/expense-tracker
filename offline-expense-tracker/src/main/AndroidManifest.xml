<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Used only to show a local notification when a transaction suggestion is found; nothing leaves the device. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OfflineExpenseTracker">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1377363250840020~8807431404" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HistoryActivity"
            android:label="Expense history"
            android:exported="false" />

        <activity
            android:name=".EditEntryActivity"
            android:label="Edit entry"
            android:exported="false" />

        <activity
            android:name=".ManageCategoriesActivity"
            android:label="Manage categories"
            android:exported="false" />

        <activity
            android:name=".SmsReviewActivity"
            android:label="SMS suggestions"
            android:exported="true">
            <!-- Lets a bank/UPI SMS be shared into the app from Messages (or any app) via the system share sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Reads only the currently-set default SMS app's notifications (bank/UPI alerts) to suggest
             expense entries. Requires the user to grant Notification access in system Settings;
             nothing leaves the device and no SMS/Call Log permission is requested. -->
        <service
            android:name=".SmsNotificationListenerService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
